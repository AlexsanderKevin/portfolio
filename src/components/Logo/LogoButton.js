import Logo from "./Logo"

const LogoButton = () => {
    const handleClick = () => {
        
    }

    return (
        <button
            onClick={ handleClick }
        >
            <Logo/>
        </button>
    )
}

export default LogoButton
